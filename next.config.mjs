/** @type {import('next').NextConfig} */
const nextConfig = {
	/* async redirects() {
		return [{
			source: '/',
			destination: '/Desktop',
			permanent: true,
		}];
	} */
};

export default nextConfig;